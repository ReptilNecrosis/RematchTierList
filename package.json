{
  "name": "rematch-tier-list",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @rematch/web",
    "build": "npm run build --workspace @rematch/web",
    "lint": "npm run lint --workspace @rematch/web",
    "test": "npm run test --workspace @rematch/rules-engine",
    "typecheck": "npm run typecheck --workspace @rematch/shared-types && npm run typecheck --workspace @rematch/rules-engine && npm run typecheck --workspace @rematch/import-adapters && npm run typecheck --workspace @rematch/discord-sync && npm run typecheck --workspace @rematch/web"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
